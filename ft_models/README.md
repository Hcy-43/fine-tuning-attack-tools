# ft_models/

LoRA adapters produced by `src/sft.py` / `scripts/sft.sh`, one subfolder per filtering strategy
and poison ratio (e.g. `random/ratio_0.3/final`). Not checked in — regenerate by running the
fine-tuning scripts against a base model in `models/`.
